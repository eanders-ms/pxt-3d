namespace threed {
    export class Point {
        public x: number;
        public y: number;
        public d: number;

        constructor(x: number, y: number, d: number) {
            this.x = x;
            this.y = y;
            this.d = d;
        }
    }
}
